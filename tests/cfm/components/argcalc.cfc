component {
  function greet(who="world") { return "hi " & who; }
  function add(a=0, b=0) { return a + b; }
  function echo(a) { return a; }
  function two(name, value) { return name & "=" & value; }
}
